apiVersion: v1
kind: Service
metadata:
  name: nimbus-web
  labels:
    app: nimbus-web
spec:
  selector:
    app: nimbus-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
